version: '3'

networks:
  proxy:
    driver: bridge

services:
  nginx:
    image: nginx
    ports:
      - 80:80
    networks: 
      - proxy

  web-blue:
    image: nginx
    networks:
      - proxy

  web-green:
    image: nginx
    networks:
      - proxy